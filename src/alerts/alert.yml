version: 2

alerts:
  - name: my_first_alert
    config:
      enabled: true
      trigger:
        type: orchestration_status
        triggered_on:
          - failure
          - success
          - partial_success
        orchestrations:
            - my_first_dbt_orchestration
      destinations:
        email:
          email_addresses:
            - adrian.barfuss@y42.com
