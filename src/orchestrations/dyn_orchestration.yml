orchestrations:
  - name: my_first_build_orchestration
    config:
      command: y42 build --select +my_first_dbt_model
      cron: 7 5-22 * * 1-5
      enabled: false
      timeout:
        hours: 4
      retries:
        max: 3
        interval:
          minutes: 4
