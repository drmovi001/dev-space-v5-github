orchestrations:
  - name: my_first_build_orchestration
    config:
      command: "y42 build --select +my_first_dbt_model"
      cron: "0/30 * 1/1 * *"
      enabled: true
      force_schedule: true
      timeout_in_seconds: 1000
      retries: 
        max: 4
        interval_in_seconds: 300
