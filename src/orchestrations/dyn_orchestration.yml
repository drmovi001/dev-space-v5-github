orchestrations:
  - name: my_first_build_orchestration
    description: my first orchestration yo
    meta:
      asset_status: deprecated
      experts:
        users:
          - pawel.paulinski@y42.com
        teams: []
    config:
      command: "y42 build --select +my_first_dbt_model"
      cron: "0/30 * 1/1 * *"
      enabled: true
      execution_timeout_in_seconds: 40223
      retries: 
        number_of_retries: 4
        time_between_retries_in_seconds: 10
