version: 2

orchestrations:
  - name: my_first_dbt_orchestration
    config:
      command: y42 build -s my_first_dbt_model
      cron: "*/30 * * * *"
      enabled: true
  - name: my_second_dbt_orchestration
    config:
      command: y42 build -s my_second_dbt_model
      cron: "*/30 * * * *"
      enabled: true
