version: 2

orchestrations:
  - name: orchestration_test_1
    config:
      command: y42 build -s my_first_dbt_model
      cron: "0 1 * * *"
  - name: orchestration_test_2
    config:
      command: y42 build -s my_first_dbt_model
      cron: "0 1 * * *"
